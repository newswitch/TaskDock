# 1.0.0 验证记录

验证环境：Windows x64、Node.js 22.21.0、Rust 1.98.1 MSVC、WebView2 152。日期：2026-09-14。

## 自动检查

- `npm test`：16 项通过，涵盖数据迁移、异常数据保护、保存失败、并发写入、备份恢复、时间语义和导入合并。
- `npm run build`：TypeScript 检查和 Vite 生产构建通过。
- `cargo test --manifest-path src-tauri/Cargo.toml --offline`：13 项 Rust 测试通过。包含实际磁盘上的 SQLite 关闭重开、不同连接的版本冲突、备份数量、数据库完整性、导出替换失败保护，以及窗口边界计算。
- `cargo fmt` 和 `git diff --check`：代码格式整理及差异检查完成。
- Windows Release 编译及 NSIS 安装器生成通过。

## 浏览器交互验证

- 旧版事项自动迁移，原始数据保留。
- 360 × 560 默认尺寸下完整显示日期表单，300 × 360 最小尺寸下可以滚动编辑。
- 创建等待事项，显示本次等待计时。
- 完成事项并显示开始、完成时间。
- 编辑已完成事项的备注，保留完成状态和时间。
- 移入回收站并恢复原状态。
- 查看保存版本，显示事项数量；覆盖恢复需勾选确认，恢复后数量与预览一致。

## 本轮修复与回归

1. 浏览器版无变化保存会把上一份有效备份覆盖成当前版本。先添加测试复现失败，再修复为不轮换备份，回归通过。
2. 原窗口恢复只保证标题栏可操作，较大窗口或边缘位置可能让底部控件超出屏幕。现在重新显示或停靠时按当前显示器工作区调整尺寸和位置。测试涵盖越界、超大尺寸、负坐标显示器和缩放后的停靠边距。
3. 导出原先直接写入目标文件，中途失败可能截断旧备份。现在先写入同目录临时文件并刷新，再替换目标；Windows 独占锁导致替换失败时，原文件保持完整，临时文件也被清理。

## 桌面交互

- 本机 Release 和安装版均成功启动，SQLite 加载正常。
- 重复启动只保留一个 TaskDock 进程，并唤回已有窗口。
- 关闭窗口后隐藏，后台进程保留；再次启动可重新显示。
- 自启开关成功开启后关闭，最终保持关闭；卸载前确认没有 TaskDock 自启注册值。
- 关闭时生成窗口位置文件；安装版重开恢复 360 × 560 大小，并把原本右侧超出屏幕 6 像素的位置修正为完全可见。
- 原生导出文件对话框打开和取消正常，取消后返回可操作的主界面。

前一轮曾被物理 Esc 中止；本轮根据“继续检测”恢复了以上检查。

## 安装与卸载

- 重新生成 Release 程序及 NSIS 安装器。
- 安装前确认没有已有 TaskDock 安装，备份本机数据和窗口配置。
- 使用 `/S /NS` 将当前用户安装包安装到项目内 `artifacts/acceptance/安装 测试`，验证含中文和空格的路径。不创建快捷方式。
- 安装器返回 0，注册版本为 1.0.0，安装位置和卸载入口正确。
- 安装后的程序与便携文件仅相差 Tauri 安装类型标记 `UNK` → `NSS` 三个字节；其余字节一致。
- 安装版启动及界面加载通过。
- 静默卸载返回 0，程序、安装目录及卸载注册项已移除。保留的测试安装路径记录另行清理。
- 卸载保留 SQLite；安装前后事项及保存版本的内容摘要一致，`PRAGMA integrity_check` 均返回 `ok`。本机数据库此次为空；非空数据库的持久化由磁盘测试单独覆盖。
- 本次没有创建或修改实际事项。验收快照和结果存于 `artifacts/acceptance/`，最终程序与安装包存于 `artifacts/`，校验和见 `SHA256SUMS.txt`。

## 仍需人工验证

- 原生自动化接口未暴露系统托盘，未直接点击托盘图标和右键菜单。
- 自启仅验证开关，未重启 Windows 或注销用户验证登录启动。
- 多屏负坐标和尺寸变化通过计算测试，未执行真实显示器热插拔及跨屏缩放交互。
- 原生拖动及边缘缩放未完成可靠的自动化验收。
- 文件对话框的焦点信息与可见输入框不一致，未继续自动输入路径；通过原生文件选择完成导出、导入的整个流程仍需人工检查。文件写入、失败保护、数据解析和恢复逻辑已有独立测试及浏览器交互验证。
- 安装向导页面和快捷方式未点击验收；安装包未签名。本地没有触发远程 CI。

## 终端外观更新

- 面板改为真正的深色半透明背景，正文、菜单、编辑、恢复及错误提示统一采用深色样式。
- 背景浓度默认 82%，可在 45%–96% 之间调整，只影响面板背景。文字保持不透明；外观偏好与事项数据分别保存。
- 浏览器实测滑块从 82% 调为 81%，刷新后仍为 81%，测试结束恢复 82%。
- 360 × 560 下检查事项卡片、时间、菜单和完整编辑表单；300 × 360 下编辑表单没有横向溢出，保存和取消按钮可以通过纵向滚动到达。
- 本轮 `npm test` 16 项通过，TypeScript 和生产前端构建通过，浏览器没有警告或错误日志。Rust 数据层没有改动，前述 13 项 Rust 检查结果沿用。
- Release 程序和安装包已重新生成，`artifacts/SHA256SUMS.txt` 已更新。打开最终桌面程序，实测后方窗口可通过面板透出，文字和按钮保持不透明。桌面程序保留打开，便于查看实际效果。

## 靠边收起与图钉

- 增加四边识别、鼠标移开后延迟收起、独立边缘箭头、悬停展开及图钉固定。角落距离相同时优先右侧，收起使用显示器工作区，兼容负坐标及缩放。
- 主窗口收起时保留原尺寸和位置；箭头窗口不参与窗口状态保存。编辑、菜单、输入操作、原生文件对话框及鼠标按键操作会阻止自动收起。手动隐藏到托盘会同时隐藏箭头。
- 图钉设置以独立文件原子保存，重新启动时读取。无法读取或遇到未来版本时保持固定，避免丢失窗口入口。
- 本轮前端 16 项、Rust 22 项测试通过，共 38 项。新测试覆盖四边识别、部分越界、负坐标与缩放、箭头可见范围、延迟取消、移动和缩放重置延迟、展开后的指针衔接，以及固定状态持久化。
- TypeScript 和前端生产构建、Windows Release 和 NSIS 打包均通过。新版已在桌面启动，图钉按钮显示正常，事项没有改动。
- 完整鼠标交互尚未验完：在切换到浏览器测试窗口以移开鼠标时，桌面自动化工具因无法可靠确认当前浏览器 URL 而停止了本轮电脑操作。停止后没有继续发出界面输入；悬停展开、移开再收起、实际点击图钉与托盘联动仍需桌面验收。

## 常驻资源检查与精简

- 对当时正在运行的上一版程序（PID 65220）及其 7 个 WebView2 子进程采样，未计入 Codex 等其他应用的 WebView2。16 个逻辑处理器，12–15 秒静置样本：整机 CPU 约 0.08%–0.15%，私有常驻内存约 113–115 MiB，私有提交量约 211–214 MiB。各进程工作集合计约 416 MiB，包含重复的共享页，不能用它当作独占物理内存。
- 可复测的只读脚本为 `scripts/measure-resources.ps1`，基线输出位于 `artifacts/resource-baseline.json`。这些数字是上一版的短时样本，不是新版本降幅或长期峰值承诺。
- 23:14 的最终复测记录为 12.01 秒、整机 CPU 0.1219%、私有常驻 87.78 MiB、私有提交 216.38 MiB。当时同时在进行 Release 构建。驻留量会随运行及系统内存管理变化；此次仍是同一个旧版进程，不能把驻留下降归功于新版优化。
- 箭头改为单独的 HTML/TypeScript 入口，生产 JS 共约 3.5 kB（原来会加载约 222 kB 的 React 共用入口）。检查构建后的 HTML 和 import，未引用主界面、React 或事项存储。脚本体积变化不能直接换算成内存降幅。
- 箭头改为首次收起时才创建，加载完成之前保留主窗口。Windows 创建过程在单独线程执行，避免事件循环等待 WebView2 时死锁。首次收起可能略慢；之后复用已创建的箭头。
- 边缘检测改为可唤醒的等待：固定、对话框阻止、最小化或主动隐藏时没有定时检查；屏幕中间和收起后每秒检查一次；边缘待收起时每 120 ms 检查。移动、缩放、焦点和交互状态变化可立即唤醒，多个通知合并且同一时刻最多一个检查。
- 取消装饰光标无限闪烁。空列表只在跨日时更新日期，有未完成事项时每分钟更新时间；隐藏时停止前端刷新。
- 前端 16 项、Rust 25 项测试通过，共 41 项。新增调度测试验证休眠后不轮询、事件唤醒、处理中通知不会丢失及合并、定时检查后再次休眠。TypeScript、前端生产构建通过。
- 本轮未重启当前用户正在运行的程序。优化版本另存于 `artifacts/low-resource/`；完整桌面悬停回归及新版资源实测仍待运行新版后进行。

## 边缘展开与收起动效

- 展开 260 ms、收起 190 ms，沿停靠方向滑动，辅以一次轻微绿色边线亮起。只动画 `transform` 和 `opacity`；没有持续动画、滤镜、动画库或逐帧跨进程通信。结束后取消动画对象并移除 `will-change`。
- 主窗口位置和尺寸保持不变，在透明宿主内完成过渡。收起结束后先将前端内容隐藏，再通知原生隐藏窗口；再次显示时从隐藏状态滑入，避免先闪出完整面板。箭头在展开完成后退出。
- 新增过渡编号与完成通知校验。鼠标返回、固定、编辑、托盘隐藏或重复唤回时可以取消旧的收起，迟到的完成通知无效。900 ms 后自动结束未确认的过渡，避免渲染器暂停导致窗口停在半途中。
- 系统减少动态效果偏好下直接切换，没有过渡动画。
- 前端 21 项、Rust 28 项测试通过，共 49 项。新测试涵盖取消、连续反向、迟到或重复通知、四个方向、减少动态效果、清除动画状态和恢复期限。
- 使用实际 App 和同一动效模块搭建独立验证页 `artifacts/motion-preview.html`，通过本地 Vite 验证，不包含在安装包中。四边收起/展开后均为活动动画 0、`will-change: auto`；实际浏览器快速反向的起点位移差 0.00 px。减少动态效果同样通过，控制台无警告和错误。慢速截图检查了滑入中途的边缘裁切和透明文字，随后关闭验证页并停止开发服务器。
- 浏览器工具第一次初始化遇到本机沙箱 ACL 错误，重置后成功恢复验证。
- 桌面新版成功启动并自动收起。向真实箭头发送零滚动量的鼠标移动后，未点击便展开了主窗口，完成后箭头消失。图钉实际点击固定成功，再次点击恢复原来的未固定状态，设置文件核对为 `pinned: false`。没有修改事项。
- 实机截图发现 Windows 默认最小跟踪宽度把窄箭头撑宽；创建箭头时显式设置最小尺寸 1 × 1，允许按 24 × 64 或 64 × 24 的实际边缘尺寸显示。
- 实际点击“隐藏到托盘”后，主窗口和箭头都从可见窗口列表消失。旧版便携程序保留在 `artifacts/pre-motion/`，最终程序与安装包更新到 `artifacts/` 和 `artifacts/smooth-motion/`，SHA-256 校验一致。最终 Release 和 NSIS 构建完成。
- 最终修正版再次启动并自动收起，桌面截图确认箭头实际为窄竖条，箭头图标完整显示，不再受系统默认最小窗口宽度影响。
- 最终版空列表、收起状态下采样 15.01 秒：TaskDock 与其 7 个 WebView2 子进程的整机 CPU 统计为 0.0%（采样精度内），私有常驻内存 112.07 MiB，私有提交量 214.55 MiB，16 个逻辑处理器。原始记录见 `artifacts/smooth-motion/resources-idle.json`。这只代表该短时静置样本，不是动画期间峰值或长期内存上限；不能据此声称内存相对旧版显著下降。

## 拖动边缘吸附（2026-09-15）

- Windows 主窗口增加原生移动消息处理。距离工作区四边 24 个逻辑像素内吸附，同一拖动中拉开超过 36 个逻辑像素后解除；角落可同时吸附两轴。按 DPI 换算，工作区避开任务栏。图钉只控制收起，不阻止吸附。
- 从拖动开始时的抓取点计算未吸附位置，防止逐像素慢拖被边缘困住；改变显示器工作区时清除旧吸附状态。窗口大小改变时按比例保留抓取点。尺寸调整、程序停靠和展开动画不执行这段吸附计算。
- 使用窗口自身的 `WM_MOVING` 消息修改系统提供的矩形，没有新增定时器、全局鼠标钩子或逐帧前端通信。窗口销毁时移除回调并释放状态。实现依据为 [Microsoft WM_MOVING](https://learn.microsoft.com/en-us/windows/win32/winmsg/wm-moving) 和 [SetWindowSubclass](https://learn.microsoft.com/en-us/windows/win32/api/commctrl/nf-commctrl-setwindowsubclass)。
- 前端 21 项、Rust 35 项测试通过，共 56 项。新增 7 项涵盖四边和角落、距离边界、慢拖解除、吸附抖动与再次捕获、负坐标、缩放、任务栏偏移、跨屏状态清除及超大窗口。TypeScript、前端生产构建、Windows Release 和 NSIS 打包通过。
- 新版已替换并启动，旧版保存在 `artifacts/pre-snap/`，程序和安装器同时保存在 `artifacts/` 与 `artifacts/edge-snap/`，SHA-256 一致。更新前没有正在编辑的事项，使用关闭请求保存了当前窗口位置；没有修改事项数据。
- 自动化快速标题栏拖拽没有观察到可确认的位移，未据此认定鼠标拖拽验收通过。改用 Windows 系统菜单“移动”进入原生移动循环，再通过鼠标位置变化触发相同的移动消息：窗口可从顶部拉至 y=166；右上角附近从 x=2220 对齐至 x=2236（宽 324，右边为 2560），再从 y=27 吸至 y=0，角落小幅移动保持不变。验证时图钉为固定，证明固定状态仍能吸附；结束系统移动后恢复 `pinned: false`。
- 一次后续鼠标检查遇到 `unknown screenshotId screenshot-0`，刷新窗口后恢复读取并结束移动模式。常规标题栏拖拽的完整手感、实际左/下边及多显示器切换仍需人工验收；对应几何与慢拖解除逻辑已由计算测试覆盖。
- 新版主界面展开、未固定时采样 15.02 秒：TaskDock 与其 6 个 WebView2 子进程整机 CPU 约 0.2406%，私有常驻内存 88.55 MiB、私有提交量 180.49 MiB（16 个逻辑处理器）。记录见 `artifacts/edge-snap/resources-idle.json`。此次没有执行拖动输入，仍有原来的边缘检测；与上一轮已创建箭头的收起状态不同，不能据此宣称内存降幅，也不是拖动峰值或长期上限。
- 后续用户试用后确认当前版本“没有问题”，并要求将代码更新到 GitHub。上述自动化覆盖范围保留作为测试方法记录。
